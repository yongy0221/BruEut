<link rel="stylesheet" href="stylesheets/forests.css">



<p id="notice"><%= notice %></p>

<%=render 'layouts/blackbar'%>

<div id="menuimage">
  <%= image_tag 'ucla-re.jpg', class: 'menuimage'%>
</div>
<!-- ///////////////게시글//////////////////////////// -->
<div class="main">
  <table class="boardtable">
    <thead>
      <tr class="pcontitle">
        <th scope="cols"><%= @pointless.title.html_safe %></th>
        <th scope = "cols"> <%= @pointless.user.name %> </th>
      </tr>
    </thead>
    <tbody>
      <tr class ="pcontitle">
        <th scope="row"><%= @pointless.content.html_safe %></td>
      </tr>
    </tbody>
  </table>
  <br>
  <br>

    <% if current_user%>

      <form action='like' method=post %>
          <input type=hidden name=pointless_id value = <%= @pointless.id %> >
          <input type=Submit value="Like <%= @pointless.like %>">
        </form> <form action='dislike' method=post %>
          <input type=hidden name=pointless_id value = <%= @pointless.id %> >
          <input type=Submit value="DisLike <%= @pointless.dislike %>">
        </form>
        <div>
        <%= link_to pointlesses_path, {:style=>'text-decoration:none;'} do%>
            <div class="btnn" style="margin-left: 0%; ">목록</div>
          <% end %>
        <% if current_user %>
         <% if @user.id == @pointless.user_id %>
          <%= link_to @pointless, method: :delete, data: { confirm: 'Are you sure?'}, :style=>'text-decoration:none;' do %>
            <div class="btnn" style="margin-left:89%; margin-top:-43px">삭제</div>
          <% end %>
        <% end %>

      </div>
      <% end %>
    <% end %>

    <br>
    <br>
    <div class="pcomment">
      <%= render @pointless.pcomments %>
      <% if current_user %>
        <%= render "pcomments/form" %>
      <% end %>
    </div>

<% @pointless.pluids.each do |p| %>
<% end %>
</div>
